-- Defines string literals for module split boundaries.

module SplitBounds where

splitOpen   = "{-- #SPLIT#"

splitClose  = "#SPLIT# --}"

splitBegin  = "-- Split begin"

splitEnd    = "-- Split end"
